
function setup() {
        canvas = createCanvas(640, 480);
        canvas.center();
        video= createCapture(VIDEO);
        video.hide();
        background(255, 258, 213)
}
function draw() {
    circle(30, 30, 20);
    background()
describe('white circle with black outline in mid of gray canvas');
}
